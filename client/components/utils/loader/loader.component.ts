//Shows a simple animated loader image to the user

import {Component} from '@angular/core'

@Component({
    moduleId: module.id,
    selector: 'loader',
    templateUrl: 'loader.html',
    styleUrls: ['loader.css']
})
export class LoaderComponent{

}